spring:
  profiles:
    active:
    - dev

---
spring:
  profiles: dev  # 开发环境
  application:
    name: config-dev

---
spring:
  profiles: test  # 开发环境
  application:
    name: config-test

# 配置文件需保存为UTF-8格式